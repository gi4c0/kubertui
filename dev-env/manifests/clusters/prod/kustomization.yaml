# Lean: apps + payments + monitoring only. Different namespace set than the others.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../components/apps
  - ../../components/payments
  - ../../components/monitoring
